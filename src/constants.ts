export const ADDON_ID = "forivall/monitor";
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PARAM_KEY = `monitor`;
export const ARGTYPES_KEY = `monitor`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
};
